language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t shadzic/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true shadzic/docker-react npm run test